"""backend.backend package shim (minimal)."""
# Keep this file minimal — it marks backend/backend as a package so submodules like
# backend.backend.app can be resolved by the import system.
__all__ = ['app']
